<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= (typeof pageTitle !== 'undefined' && pageTitle) ? pageTitle : 'Admin Login – Keterbukaan Informasi Lapas' %></title>
  <link rel="stylesheet" href="/css/admin.css">
</head>
<body>
<div class="login-page">
  <div class="login-box">
    <div class="login-logo">
      <h1>🏛 LAPAS</h1>
      <% const subtitle = String((typeof loginSubtitle !== 'undefined' && loginSubtitle) ? loginSubtitle : 'Sistem Keterbukaan Informasi Narapidana\nPanel Administrasi'); %>
      <p><%- subtitle.replace(/\n/g, '<br>') %></p>
    </div>
    <% if (error) { %>
    <div class="alert alert-danger">⚠️ <%= error %></div>
    <% } %>
    <form method="POST" action="<%= (typeof formAction !== 'undefined' && formAction) ? formAction : '/admin/login' %>">
      <div class="form-group">
        <label for="username">Username</label>
        <input type="text" id="username" name="username" placeholder="Masukkan username" required autofocus>
      </div>
      <div class="form-group">
        <label for="password">Password</label>
        <input type="password" id="password" name="password" placeholder="Masukkan password" required>
      </div>
      <br>
      <button type="submit" class="btn btn-primary">🔐 Masuk</button>
    </form>
    <p style="text-align:center;margin-top:16px;font-size:11px;color:#aaa"><%= (typeof helperText !== 'undefined' && helperText) ? helperText : 'Default: admin / admin123' %></p>
  </div>
</div>
</body>
</html>
